{
  "name": "screencast",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "homepage": "https://srujanpalled.github.io/screencast",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npx gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^8.0.12"
  },
  "dependencies": {
    "peerjs": "^1.5.5"
  }
}
